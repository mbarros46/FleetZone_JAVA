# Profile de desenvolvimento SEM Oracle - Para testes de autenticação JWT
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_ON_EXIT=FALSE;DB_CLOSE_DELAY=-1
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# Configurações específicas para H2
spring.h2.console.settings.web-allow-others=true
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# JWT
jwt.secret=H2Qf7wqz0L3c4xZ9rP6sM8tA1bC5vN7kY2pR4uT6wX8z
jwt.expiration=86400000

# Desliga o restart do DevTools
spring.devtools.restart.enabled=false

# CORS configurado no SecurityConfig.java

# Remove configurações específicas do Oracle para H2
# Remove oracle dialect que estava causando conflito